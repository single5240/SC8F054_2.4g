# QW-247 SC8F054 接收端工程指南

## 项目定位

- 本工程是基于 `SC8F054` 的 QW-247 2.4 GHz 灯具接收端固件，系统时钟为内部 16 MHz。
- 外设包括 XL2400T 无线收发芯片、三路 RGB PWM 和一个本地按键。
- 当前产品不带电机。不得加入、移植或恢复电机驱动、电机状态机、DW7802 通信及协议命令 `0x50`。
- 必须保留低功耗流程：长按按键主动休眠、约 30 分钟无操作自动休眠、按键电平变化唤醒，以及唤醒后的系统和 XL2400T 接收状态恢复。
- `FEATURE_RF_RESET_ENABLE`、`FEATURE_RF_TX_ENABLE`、`FEATURE_SOFT_UART_ENABLE` 默认均为 `0`；需要相关功能时通过这些宏启用，不要直接删除条件编译。

## 工具链与资源约束

- 工程文件：`SC8F054_timer_C.scw`，目标芯片由工程配置为 `SC8F054`。
- 使用 SCMCU IDE V2.00.17 Beta3 附带的 Microchip MPLAB XC8 V1.45 编译器；工程采用速度优化并关闭调试信息。未经要求不要升级编译器、替换芯片头文件或改动工程格式。
- 编译器较旧，按 C90 风格编写：局部变量放在代码块开头，避免依赖 C99/C11 语法和新库特性。
- 当前工程配置基线约占用 1994/2048 words Flash（97.4%）和 114/160 bytes RAM（71.2%）。新增功能前必须评估空间，优先复用现有状态和位域，不使用动态内存，避免 `stdlib`、随机数库、递归、大型查表和复杂抽象。
- 现有 `.c/.h` 文件为 GBK、CRLF。修改时保持原编码和换行，避免整文件编码转换造成无关差异；`AGENTS.md` 使用 UTF-8。

## 主要文件职责

- `SC8F054_main.c`：主循环、Timer2 中断、灯效时基和低功耗计时。
- `SC8F054_init.c`：系统、GPIO、PWM 初始化及休眠/唤醒流程。
- `SC8F054_key.c`：按键扫描、消抖、长短按事件和本地颜色切换。
- `SC8F054_led.c`：RGB 颜色表、10 位 PWM 占空比和渐变目标更新。
- `SC8F054_xl2400t.c`、`xl2400t.h`：XL2400T 软件 SPI、寄存器配置、收包和 RX 模式切换。
- `SC8F054_soft.c`：5 字节无线业务帧校验、去重、逻辑通道过滤和灯效命令解析。
- `SC8F054_var.h`：全局控制结构、状态位和函数声明。
- `SC8F054_define.h`：引脚、按键阈值、颜色、灯效模式及默认逻辑通道定义。

## 硬件与并发注意事项

- `RB0` 同时定义为 `KEY` 和 XL2400T 的 `CSN`，属于既有的分时复用设计。无线收包流程会切换 `TRISB`；修改按键或 SPI 代码时必须检查方向切换、端口恢复和调用时序，不能把两者当作独立引脚。
- RGB PWM 分配为：绿色 PWM1/RB1、红色 PWM2/RB3、蓝色 PWM4/RB2。占空比为 10 位，更新时必须同时正确处理高 2 位和低 8 位。
- Timer2 中断提供约 1 ms 基础时基。中断中只做短小、确定时间的计数和灯效更新，不加入阻塞延时、无线通信或复杂计算。
- 涉及共享状态、位域、计数器和端口方向时，检查中断与主循环之间的竞态、计数溢出和更新顺序。
- XL2400T 进入休眠前会掉电。MCU 唤醒并重新执行 `Init_System()` 后，必须调用 `RF_Rx_Mode()` 恢复接收，不能只恢复 MCU 外设。

## 无线业务协议

- XL2400T 有效载荷固定为 5 字节。读取前必须确认载荷长度为 5；长度异常时清空 RX FIFO，禁止越界读取。
- 校验关系：`(data[0] + data[1] + data[2] + data[3]) & 0xFF == data[4]`。
- 默认逻辑通道为 14。通道 1～8 使用 `data[0]` 位图，通道 9～16 使用 `data[1]` 位图；位移前必须验证通道范围。
- 普通控制帧需要过滤完全相同的重复帧；DMX 命令 `0xA0` 必须逐帧处理，不参与普通帧去重。
- 当前非电机命令包括 `0x20`、`0x40`、`0x60`、`0x80`、`0xA0`、`0xB0`、`0xC0`、`0xD0`、`0xE0`、`0xF0`。
- `0x40` 流水帧连续更新通道位图时不能重复复位灯效相位；切换到其他命令时再清除流水状态。
- 按键锁定仅由本机命中或全局有效的灯效命令触发；`0x60` 只有选中本机时才能解锁。
- 新增或调整协议时，必须先确认发送端帧定义，不得猜测保留位、寄存器或电机字段含义。

## 低功耗与本地按键

- 未被无线命令锁定时，短按依次切换熄灭、15 种固定颜色、15 色轮播，再回到熄灭。
- 未被锁定时，长按关闭 RGB 并置位休眠标志。
- 有效控制活动会刷新无操作计时；约 30 分钟未操作进入休眠。修改 Timer2 分频或软件时基后，必须重新核算该超时。
- 进入休眠前清除收包长度、去重和流水状态，关闭 PWM/中断并使 XL2400T 掉电；唤醒后恢复 GPIO、PWM、定时器、无线 RX 和本地红灯状态。
- 修改休眠代码时重点检查按键释放等待、RB0 电平变化中断标志、看门狗、唤醒源以及休眠前后的端口方向，避免无法休眠或无法唤醒。

## 修改原则

- 保持改动小而明确，不重排无关代码，不批量格式化，不更换现有中文注释风格。
- 不修改未确认的数据手册寄存器值；若必须调整 SC8F054 或 XL2400T 寄存器，说明依据、时序和可能影响。
- 重点检查无线长度与校验、数组边界、位移范围、PWM 溢出、计数器溢出、重复帧状态以及掉电恢复。
- `output/`、`.lst`、`.lpp`、`.ram`、`.hex`、`.bin`、`.obj` 等为编译产物；除非用户明确要求发布固件或更新工程产物，不要主动修改或提交它们。
- Git Commit 使用简洁、明确的中文，提交前检查 `git diff --check` 和 `git status --short`，确保没有无关文件。

## 验证要求

每次修改至少执行：

1. 使用 `SC8F054_timer_C.scw` 和原 SCMCU/XC8 工具链完整编译，确认无错误并记录 Flash/RAM 占用。
2. 扫描源码，确认未出现 `0x50`、`DW7802` 或其他电机实现。
3. 协议修改后测试正确帧、错误校验和、非 5 字节载荷、重复普通帧、连续 DMX 帧以及通道 1/8/9/16 边界。
4. 灯效修改后测试 RGB 三路、常亮、慢闪、快闪、频闪、呼吸、流水、间隔快闪和 15 色轮播。
5. 低功耗修改后实测长按休眠、自动休眠、按键唤醒及唤醒后的第一帧无线控制。

如果没有实物测试条件，必须明确说明只完成了编译/静态验证，不能将板级行为描述为已确认。
